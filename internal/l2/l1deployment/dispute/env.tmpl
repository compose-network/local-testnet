# Deployer private key
DEPLOYER_PRIVATE_KEY={{.DeployerPrivateKey}}

# Etherscan API key (not needed for localnet, but required by scripts)
ETHERSCAN_API_KEY=

# DisputeGameFactory address (populated after deployment)
HOODI_GAME_FACTORY_ADDRESS=
